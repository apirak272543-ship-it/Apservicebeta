(() => {
  'use strict';
  const M = window.APServiceMPA;
  if (!M) return;
  const runtime = () => window.APServiceAdminRuntime;
  const esc = value => M.ui.escapeHtml(String(value ?? '')); const stableJson = value => JSON.stringify(value, (_, item) => item && typeof item === 'object' && !Array.isArray(item) ? Object.keys(item).sort().reduce((out, key) => { out[key] = item[key]; return out; }, {}) : item);
  const cleanUrl = value => /^https:\/\//i.test(String(value || '').trim()) ? String(value).trim() : '';
  const pageChoices = [
    ['default', 'ค่าเริ่มต้นทุกหน้า'], ['home', 'หน้าแรก'], ['stores', 'ร้านค้า'], ['store', 'รายละเอียดร้านค้า'], ['orders', 'รายการออร์เดอร์'], ['order', 'รายละเอียดออร์เดอร์'], ['checkout', 'ชำระเงิน'], ['profile', 'โปรไฟล์'], ['notifications', 'แจ้งเตือน'], ['support', 'ช่วยเหลือ'], ['parcel', 'พัสดุ'], ['retail', 'Retail'], ['retail-checkout', 'Retail Checkout'], ['marketplace', 'ตลาดชุมชน'], ['marketplace-item', 'รายละเอียดสินค้า'], ['marketplace-new', 'ลงประกาศสินค้า'], ['marketplace-profile', 'โปรไฟล์ตลาด'], ['marketplace-chat', 'แชตตลาด'], ['register', 'สมัครสมาชิก'], ['recover', 'กู้คืนบัญชี'], ['update-password', 'ตั้งรหัสผ่านใหม่'], ['privacy', 'นโยบายความเป็นส่วนตัว']
  ];
  const motionChoices = [['none', 'ไม่ใช้ Motion'], ['summer', 'ฤดูร้อน · แสงอุ่นเบา ๆ'], ['rainy', 'ฤดูฝน · หยดฝน'], ['spring', 'ฤดูใบไม้ผลิ · กลีบดอกไม้'], ['songkran', 'สงกรานต์ · ละอองน้ำ'], ['loy_krathong', 'ลอยกระทง · แสงเทียนและประกาย'], ['christmas', 'คริสต์มาส · หิมะและประกายไฟ'], ['new_year', 'ปีใหม่ · พลุและแสงเฉลิมฉลอง'], ['valentines', 'วาเลนไทน์ · หัวใจลอย'], ['halloween', 'ฮาโลวีน · หมอกและค้างคาว'], ['lunar_new_year', 'ตรุษจีน · โคมไฟและประกายทอง'], ['ramadan_eid', 'รอมฎอน/อีด · ดาวและแสงจันทร์'], ['diwali', 'ดิวาลี · แสงประทีป'], ['winter', 'ฤดูหนาว · หิมะบาง ๆ']];
  const directionChoices = [['down', 'ตกจากบนลงล่าง'], ['left', 'ลมพัดจากขวาไปซ้าย'], ['right', 'ลมพัดจากซ้ายไปขวา'], ['diagonal', 'เฉียงแบบพายุ'], ['up', 'ลอยจากล่างขึ้นบน']];
  const festivalChoices = [['', 'ไม่ผูกกับเทศกาล'], ['songkran', 'สงกรานต์'], ['loy_krathong', 'ลอยกระทง'], ['christmas', 'คริสต์มาส'], ['new_year', 'ปีใหม่สากล'], ['valentines', 'วาเลนไทน์'], ['halloween', 'ฮาโลวีน'], ['lunar_new_year', 'ตรุษจีน'], ['ramadan_eid', 'รอมฎอนและอีด'], ['diwali', 'ดิวาลี'], ['summer', 'ฤดูร้อน'], ['rainy', 'ฤดูฝน'], ['winter', 'ฤดูหนาว']];
  const defaults = () => ({ version: 1, default: { backgroundUrl: '', overlay: 0.86, position: 'center', size: 'cover', motion: 'none', direction: 'down', effectUrl: '' }, festival: { key: '', motion: 'none', direction: 'down', effectUrl: '', active: false, startsAt: '', endsAt: '' }, pages: {} });
  const normalize = value => { const base = defaults(); const source = value && typeof value === 'object' ? value : {}; return { ...base, ...source, default: { ...base.default, ...(source.default || {}) }, festival: { ...base.festival, ...(source.festival || {}) }, pages: source.pages && typeof source.pages === 'object' ? source.pages : {} }; };
  const select = (name, value, choices, extra = '') => `<select name="${esc(name)}" ${extra}>${choices.map(([key, label]) => `<option value="${esc(key)}" ${key === value ? 'selected' : ''}>${esc(label)}</option>`).join('')}</select>`;
  const field = (label, name, value, type = 'text', extra = '') => `<label class="mpa-field"><span>${esc(label)}</span><input name="${esc(name)}" type="${type}" value="${esc(value)}" ${extra}></label>`;
  const backgroundInput = (name, value, label) => `<div class="admin-content-media-field customer-visual-media-field"><span class="admin-content-media-label">${esc(label)}</span><div class="admin-media-source-actions"><label class="mpa-button mpa-button-secondary">เลือกจากคลังภาพ<input hidden type="file" accept="image/jpeg,image/png,image/webp" data-visual-file data-visual-field="${esc(name)}"></label><label class="mpa-button mpa-button-secondary">ถ่ายรูปด้วยกล้อง<input hidden type="file" accept="image/jpeg,image/png,image/webp" capture="environment" data-visual-file data-visual-field="${esc(name)}"></label></div><input hidden name="${esc(name)}" data-visual-url="${esc(name)}" value="${esc(value)}"><div class="admin-content-media-preview" data-visual-preview="${esc(name)}">${value ? `<img src="${esc(value)}" alt="ตัวอย่างพื้นหลัง ${esc(label)}">` : '<span class="mpa-muted">ยังไม่มีภาพพื้นหลัง</span>'}</div><small class="mpa-muted" data-visual-status="${esc(name)}">รองรับ JPG/PNG/WebP · ระบบบีบอัดก่อนอัปโหลดและตรวจขนาดไม่เกิน 1 MB</small></div>`;
  const saveButton = label => `<div class="admin-modal-actions customer-visual-save-row"><span class="mpa-muted" data-visual-save-status>ยังไม่ได้บันทึกการเปลี่ยนแปลงชุดนี้</span><button class="mpa-button" type="submit">บันทึก ${esc(label)}</button></div>`;
  const pageCard = (key, label, page, current) => `<form class="mpa-card customer-visual-page-card customer-visual-independent-form" data-visual-form="page" data-visual-key="${esc(key)}"><div class="mpa-page-head"><div><span class="admin-kicker">CUSTOMER PAGE · ${esc(key)}</span><h3>${esc(label)}</h3><p class="mpa-muted">การ์ดนี้บันทึกเฉพาะหน้า ${esc(label)} เท่านั้น ไม่ทับค่าหน้าอื่น</p></div><span class="admin-inline-count">แยกบันทึก</span></div><div class="admin-form-grid">${backgroundInput(`page.${key}.backgroundUrl`, cleanUrl(page.backgroundUrl), label)}<label class="mpa-field"><span>Motion ของหน้านี้</span>${select(`page.${key}.motion`, page.motion, [['inherit', 'ใช้ Motion เทศกาล/ค่าเริ่มต้น'], ...motionChoices])}</label>${field('ความเข้มชั้นขาวทับภาพ 0–1', `page.${key}.overlay`, page.overlay, 'number', 'min="0" max="1" step="0.01"')}${field('ตำแหน่งภาพ', `page.${key}.position`, page.position)}${field('ขนาดภาพ', `page.${key}.size`, page.size)}<label class="mpa-field"><span>ทิศทาง Motion</span>${select(`page.${key}.direction`, page.direction || 'down', [['inherit', 'ใช้ทิศทางค่าเริ่มต้น'], ...directionChoices])}</label></div>${saveButton(`หน้า ${label}`)}</form>`;
  function render(host, config) {
    const value = normalize(config);
    const pageRows = pageChoices.filter(([key]) => key !== 'default').map(([key, label]) => { const page = { backgroundUrl: '', overlay: value.default.overlay, position: value.default.position, size: value.default.size, motion: 'inherit', direction: 'inherit', effectUrl: '', ...(value.pages[key] || {}) }; return pageCard(key, label, page, value); }).join('');
    host.innerHTML = `<div class="customer-visual-settings"><div class="mpa-page-head"><div><span class="admin-kicker">CUSTOMER VISUAL SYSTEM</span><h2 style="margin:0">พื้นหลังและ Motion ของ Customer App</h2><p class="mpa-muted">แต่ละการ์ดมีปุ่มอัปโหลด พรีวิว และบันทึกเป็นของตัวเอง ค่าในหน้าอื่นจะไม่ถูกเขียนทับ</p></div></div><form class="mpa-card customer-visual-independent-form" data-visual-form="default"><div class="mpa-page-head"><div><h3>ค่าเริ่มต้นทุกหน้า</h3><p class="mpa-muted">ชุดนี้ใช้เป็น fallback เท่านั้น และบันทึกแยกจากทุกหน้ารายการ</p></div><span class="admin-inline-count">แยกบันทึก</span></div><div class="admin-form-grid">${backgroundInput('default.backgroundUrl', cleanUrl(value.default.backgroundUrl), 'พื้นหลังเริ่มต้นทุกหน้า')}${field('ความเข้มชั้นขาวทับภาพ 0–1', 'default.overlay', value.default.overlay, 'number', 'min="0" max="1" step="0.01"')}${field('ตำแหน่งภาพ', 'default.position', value.default.position)}${field('ขนาดภาพ', 'default.size', value.default.size)}<label class="mpa-field"><span>Motion ค่าเริ่มต้น</span>${select('default.motion', value.default.motion, motionChoices)}</label><label class="mpa-field"><span>ทิศทาง Motion ค่าเริ่มต้น</span>${select('default.direction', value.default.direction || 'down', directionChoices)}</label></div>${saveButton('ค่าเริ่มต้น')}</form><form class="mpa-card customer-visual-independent-form" data-visual-form="festival"><div class="mpa-page-head"><div><h3>เทศกาลและฤดูกาล</h3><p class="mpa-muted">บันทึกชุดเทศกาลแยกต่างหาก ไม่ทับพื้นหลังของแต่ละหน้า</p></div><span class="admin-inline-count">แยกบันทึก</span></div><div class="admin-form-grid"><label class="mpa-field"><span>เทศกาลที่กำลังเลือก</span>${select('festival.key', value.festival.key, festivalChoices)}</label><label class="mpa-field"><span>Motion ของเทศกาล</span>${select('festival.motion', value.festival.motion, motionChoices)}</label><label class="mpa-field"><span>ทิศทาง Motion เทศกาล</span>${select('festival.direction', value.festival.direction || 'down', directionChoices)}</label><label class="mpa-field"><span>เปิดใช้ Motion เทศกาล</span>${select('festival.active', String(value.festival.active), [['true', 'เปิด'], ['false', 'ปิด']])}</label>${field('เริ่มใช้เทศกาล (ไม่บังคับ)', 'festival.startsAt', value.festival.startsAt, 'datetime-local')}${field('สิ้นสุดเทศกาล (ไม่บังคับ)', 'festival.endsAt', value.festival.endsAt, 'datetime-local')}</div>${saveButton('เทศกาล')}</form><section class="mpa-card"><div class="mpa-page-head"><div><h3>พื้นหลังแยกตามหน้า</h3><p class="mpa-muted">ทุกหน้าเป็นรายการอิสระ มีปุ่มบันทึกของตัวเอง</p></div><span class="admin-inline-count">${pageChoices.length - 1} รายการ</span></div><div class="customer-visual-page-list">${pageRows}</div></section></div>`;
    attach(host);
  }
  const readScope = (form, current) => { const control = name => form.querySelector(`[name="${CSS.escape(name)}"]`); const value = name => String(control(name)?.value || '').trim(); const number = name => { const n = Number(value(name)); return Number.isFinite(n) ? Math.max(0, Math.min(1, n)) : 0.86; }; const next = normalize(current); const scope = form.dataset.visualForm; if (scope === 'default') next.default = { backgroundUrl: cleanUrl(value('default.backgroundUrl')), overlay: number('default.overlay'), position: value('default.position') || 'center', size: value('default.size') || 'cover', motion: value('default.motion') || 'none', direction: value('default.direction') || 'down', effectUrl: cleanUrl(value('default.effectUrl')) }; else if (scope === 'festival') next.festival = { key: value('festival.key'), motion: value('festival.motion') || 'none', direction: value('festival.direction') || 'down', effectUrl: cleanUrl(value('festival.effectUrl')), active: value('festival.active') === 'true', startsAt: value('festival.startsAt'), endsAt: value('festival.endsAt') }; else { const key = form.dataset.visualKey; next.pages[key] = { backgroundUrl: cleanUrl(value(`page.${key}.backgroundUrl`)), overlay: number(`page.${key}.overlay`), position: value(`page.${key}.position`) || 'center', size: value(`page.${key}.size`) || 'cover', motion: value(`page.${key}.motion`) || 'inherit', direction: value(`page.${key}.direction`) || 'inherit', effectUrl: cleanUrl(value(`page.${key}.effectUrl`)) }; } return next; };
  async function saveScope(form) { const button = form.querySelector('[type="submit"]'); const status = form.querySelector('[data-visual-save-status]'); button.disabled = true; try { const rows = await M.request('platform_configs?select=value&key=eq.customer_visuals&limit=1', { private: true, forceFresh: true, cacheKey: 'admin-customer-visuals-current' }); const next = readScope(form, rows?.[0]?.value); await M.request('platform_configs?on_conflict=key', { method: 'POST', private: true, headers: { Prefer: 'resolution=merge-duplicates,return=minimal' }, body: JSON.stringify([{ key: 'customer_visuals', value: next, updated_at: M.ui.nowIso() }]) }); const savedRows = await M.request('platform_configs?select=value&key=eq.customer_visuals&limit=1', { private: true, forceFresh: true, cacheKey: 'admin-customer-visuals-after-save' }); const saved = savedRows?.[0]?.value; if (!saved || stableJson(saved) !== stableJson(next)) throw new Error('บันทึกแล้วแต่ตรวจสอบค่าภาพจากฐานข้อมูลไม่ตรงกัน กรุณาลองใหม่'); await cleanupPendingMedia(form); await M.request('admin_action_audit', { method: 'POST', private: true, headers: { Prefer: 'return=minimal' }, body: JSON.stringify([{ actor_id: runtime()?.user?.id, action: 'customer_visual_settings_updated', reason: `บันทึก ${form.dataset.visualForm}${form.dataset.visualKey ? `:${form.dataset.visualKey}` : ''}`, before_state: rows?.[0]?.value || {}, after_state: next, created_at: M.ui.nowIso() }]) }).catch(() => null); if (status) status.textContent = 'บันทึกชุดนี้แล้ว'; M.ui.setNotice('บันทึกการตั้งค่ารายการนี้แล้ว'); } catch (error) { if (status) status.textContent = error.message || 'บันทึกไม่สำเร็จ'; M.ui.setNotice(error.message || 'บันทึกไม่สำเร็จ', 'error'); } finally { button.disabled = false; } }
  async function cleanupPendingMedia(form) { const session = await M.auth.refreshSession(false); const accessToken = session?.access_token || M.auth.getSession?.()?.access_token; const actorId = session?.user?.id || runtime()?.user?.id; if (!accessToken || !actorId || !window.APServiceMedia?.cleanupReplacedPublicMedia) return; for (const input of form.querySelectorAll('[data-visual-url][data-previous-media-url]')) { try { await window.APServiceMedia.cleanupReplacedPublicMedia({ ...M.config, accessToken, actorId, oldUrl: input.dataset.previousMediaUrl, newUrl: input.value, bucket: 'catalog-media', pathPrefix: 'admin/customer-visuals' }); } catch (error) { console.warn('ลบภาพพื้นหลังเก่าหลังแทนที่ไม่สำเร็จ', error); } delete input.dataset.previousMediaUrl; delete input.dataset.pendingMediaPath; } }

  function attach(host) { host.querySelectorAll('[data-visual-file]').forEach(input => input.addEventListener('change', async event => { const file = event.target.files?.[0]; if (!file) return; const name = input.dataset.visualField; const preview = host.querySelector(`[data-visual-preview="${CSS.escape(name)}"]`); const status = host.querySelector(`[data-visual-status="${CSS.escape(name)}"]`); const url = input.closest('form')?.querySelector(`[data-visual-url="${CSS.escape(name)}"]`); const local = URL.createObjectURL(file); if (preview) preview.innerHTML = `<img src="${esc(local)}" alt="ตัวอย่างภาพก่อนอัปโหลด">`; if (status) status.textContent = `กำลังบีบอัดและอัปโหลด ${file.name}…`; try { const session = await M.auth.refreshSession(false); const accessToken = session?.access_token || M.auth.getSession?.()?.access_token; const adminRuntime = runtime(); const actorId = session?.user?.id || adminRuntime?.user?.id; if (!accessToken || !actorId) throw new Error('เซสชัน Admin ไม่พร้อมสำหรับอัปโหลด'); const uploaded = await window.APServiceMedia.uploadPublicImage(file, { url: M.config.url, publishableKey: M.config.publishableKey, accessToken, actorId, bucket: 'catalog-media', scope: 'customer-visuals', pathPrefix: 'admin/customer-visuals', mediaType: 'CUSTOMER_BACKGROUND', ownerType: 'admin', variant: name, legacySource: { field: name, source: 'customer-visual-settings' } });     if (url) { const previousUrl = url.value || ''; if (previousUrl && previousUrl !== uploaded.publicUrl) url.dataset.previousMediaUrl = previousUrl; url.dataset.pendingMediaPath = uploaded.path || ''; url.value = uploaded.publicUrl; } if (preview) preview.innerHTML = `<img src="${esc(uploaded.publicUrl)}" alt="ตัวอย่างภาพที่อัปโหลดแล้ว">`; if (status) status.textContent = `อัปโหลดแล้ว · ${Math.ceil(Number(uploaded.bytes || 0) / 1024)} KB · กดบันทึกการ์ดนี้เพื่อยืนยัน`; M.ui.setNotice('อัปโหลดภาพแล้ว กรุณากดปุ่มบันทึกของการ์ดนี้'); } catch (error) { if (status) status.textContent = error.message || 'อัปโหลดไม่สำเร็จ'; M.ui.setNotice(error.message || 'อัปโหลดไม่สำเร็จ', 'error'); } finally { URL.revokeObjectURL(local); event.target.value = ''; } })); host.querySelectorAll('[data-visual-form]').forEach(form => form.addEventListener('submit', event => { event.preventDefault(); saveScope(form); })); }
  function mountWhenReady() { const mount = () => { if (!runtime()) return; const host = document.querySelector('#contentStudioHost'); if (!host || document.querySelector('#customerVisualSettingsMount') || !host.querySelector('#customerContentForm')) return; const section = document.createElement('section'); section.id = 'customerVisualSettingsMount'; section.className = 'customer-visual-settings-mount'; section.innerHTML = M.ui.loading('กำลังโหลดระบบพื้นหลังและ Motion…'); host.parentElement?.append(section); M.request('platform_configs?select=value&key=eq.customer_visuals&limit=1', { private: true, forceFresh: true }).then(rows => render(section, rows?.[0]?.value)).catch(error => { section.innerHTML = M.ui.error('โหลดการตั้งค่าพื้นหลังไม่สำเร็จ', error.message); }); }; const observer = new MutationObserver(mount); observer.observe(document.body, { childList: true, subtree: true }); mount(); }
  mountWhenReady();
})();
