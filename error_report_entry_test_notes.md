# ผลตรวจทางเข้าการแจ้งปัญหา

Store Console ไม่มีปุ่มแจ้งปัญหาแบบลอยบนหน้าจอเข้าสู่ระบบหรือหน้าใช้งานหลักแล้ว โดย CSS ซ่อนปุ่มเดิมและย้ายทางเข้าไว้ในเมนูรองของแท็บหน้าร้านและเวลา การทดสอบในเบราว์เซอร์ยืนยันว่าปุ่มลอยมีค่า `display: none` ขณะที่ฟังก์ชันเปิดฟอร์มจากเมนูรองยังพร้อมใช้งาน

เมื่อสร้างและเปิด overlay ทดสอบ การแตะที่ฉากหลังจะปิด overlay ได้ทันที จึงไม่เกิดสภาวะที่ชั้น overlay ค้างและบล็อกการกดหรือการเลื่อนหน้าหลัก ผู้ใช้ยังสามารถปิดด้วยปุ่มยกเลิกหรือ Escape ได้ในทั้งสามคอนโซลตามโค้ดร่วมกัน

Rider Console ให้ผลตรวจแบบเดียวกัน: ปุ่มลอยมีค่า `display: none` และฟังก์ชันเปิดฟอร์มจากเมนู Settings พร้อมใช้งาน การแตะฉากหลังของ overlay ที่เปิดเพื่อทดสอบทำให้ overlay ปิดทันที

Customer/Admin Console ให้ผลตรวจครบถ้วนเช่นกัน: ปุ่มลอยถูกซ่อนด้วย `display: none` ฟังก์ชันเปิดฟอร์มจากเมนูโปรไฟล์ยังพร้อมใช้ และการแตะฉากหลังของ overlay ปิดฟอร์มได้ทันที

การทดสอบเพิ่มด้วยปุ่ม Escape ใน Customer/Admin ปิดฟอร์มได้สำเร็จ และ `body` ยังคงรับการโต้ตอบตามปกติ จึงไม่เหลือชั้น overlay ที่ขวางหน้าจอหลังปิดฟอร์ม
