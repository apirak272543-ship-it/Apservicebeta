# ผลทดสอบศูนย์ติดตามข้อผิดพลาด

| รายการตรวจ | ผล |
|---|---|
| Syntax ของ Customer/Admin, Rider และ Store | ผ่าน |
| Contract ของแพลตฟอร์มและ error monitor | ผ่าน 35 รายการ |
| การบันทึก error | ดักเฉพาะ `error`, `unhandledrejection` และคำขอ Supabase ที่ล้มเหลว |
| การลดคำขอซ้ำ | รวม error เดิมภายใน 120 วินาทีต่อหน้า |
| การติดตาม event ทั่วไป | ไม่มี listener สำหรับ click, mousemove หรือ scroll ใน ErrorMonitor |
| ภาพหลักฐาน | บีบอัดต่ำกว่า 1 MB, private storage, จำกัดเฉพาะ Master Admin |
| การแก้โค้ด/เผยแพร่ | ไม่มีการทำอัตโนมัติ ต้องอนุมัติเป็นรายขั้นตอน |

> หมายเหตุ: คิว `approved_for_review` ทำหน้าที่บันทึกการอนุมัติให้ตรวจสอบเท่านั้น และไม่เรียกใช้บริการภายนอกหรือเปลี่ยนแปลงโค้ดเอง

## ผลตรวจหน้าจอ Customer

เปิดไฟล์ Customer/Admin ในมุมมองเบราว์เซอร์แล้วพบปุ่ม `⚑ แจ้งปัญหา` อยู่ในหน้าจอ และเมื่อเปิดฟอร์ม ระบบแสดงคำเตือนให้เบลอข้อมูลส่วนบุคคล ช่องสรุปปัญหา รายละเอียด ช่องแนบภาพ และ checkbox ยืนยันก่อนส่งครบถ้วน

## ผลตรวจ Supabase

ตรวจยืนยันแล้วว่า bucket `error-evidence` เป็น private และจำกัดขนาดไฟล์ 2 MB ตาม migration ตาราง `error_reports` มีนโยบาย INSERT, SELECT และ UPDATE แยกหน้าที่ชัดเจน นอกจากนี้การทดสอบข้อความจำลองยืนยันว่ารหัสผ่าน, token, อีเมล และหมายเลขโทรศัพท์ถูกแทนที่ด้วยค่า `[REDACTED]` ก่อนเก็บลงฐานข้อมูล
