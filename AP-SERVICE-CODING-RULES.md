# AP Service — กฎเหล็กการแก้ไขโค้ดและ UI

เอกสารนี้เป็นกฎกลางสำหรับทุกแอปและทุก repository ของ AP Service เพื่อป้องกันการแก้ปัญหาด้วยการวางโค้ดทับของเดิมจนเกิดความซ้ำซ้อนและดูแลต่อยาก

## หลักการบังคับใช้

เมื่อปรับปรุงฟีเจอร์หรือ UI ต้องค้นหาและแก้ **source of truth เดิม** โดยตรงก่อนเสมอ ห้ามสร้าง script patch, runtime override, stylesheet ซ้อน, component ซ้ำ หรือ layout ชุดใหม่มาทับ behavior เดิมเพียงเพื่อให้หน้าใดหน้าหนึ่งดูเหมือนแก้แล้ว

ห้ามเพิ่ม handler สำหรับปุ่มหรือ form ถ้ามี handler เดิมอยู่แล้ว ต้องแก้ handler เดิมให้ถูกต้อง หรือรวม handler ให้เหลือจุดเดียวและลบตัวที่หมดหน้าที่ พร้อมตรวจทุก caller และ route ที่เกี่ยวข้อง

ห้ามสร้างหน้าใหม่เพื่อแทนหน้าเดิม หากหน้าเดิมยังเป็น route หลัก ให้แก้หน้าเดิมและคง URL เดิมไว้ หากจำเป็นต้องย้าย route ต้องมี redirect/compatibility ที่ตรวจสอบแล้วและต้องระบุเหตุผลใน change note

ห้ามแก้ด้วย CSS ที่มี specificity สูงขึ้นเพื่อกลบ CSS เก่าโดยไม่รวมกฎเดิม หากพบ stylesheet หลายชุดควบคุม selector เดียวกัน ต้องกำหนด owner ของ selector เหล่านั้นให้เหลือชุดเดียว แล้วลบกฎหรือไฟล์ที่หมดหน้าที่

ห้ามเพิ่ม code path ใหม่โดยไม่ตรวจว่าฟีเจอร์เดียวกันมีอยู่แล้วใน page, patch, shared runtime, native wrapper หรือ backend อื่นหรือไม่ หากมีของเดิม ให้รวมและย้าย logic กลับไปยัง source หลักแทนการเพิ่มชั้นใหม่

## ขั้นตอนก่อนแก้

ต้องทำ inventory ของ entry point, caller, route, event handler, API/RPC และ stylesheet ที่เกี่ยวข้องก่อนทุกครั้ง ต้องบันทึกว่าจะแก้ source ไฟล์ใด ลบหรือรวมไฟล์ใด และเพราะเหตุใด ไฟล์ที่ยังพิสูจน์ไม่ได้ว่าไม่ใช้ต้องไม่ลบ

## ขั้นตอนหลังแก้

ทุก change ต้องตรวจ `git diff` ให้เห็นว่าเป็นการแก้ต้นทางหรือการรวมโค้ด ไม่ใช่การเขียนทับด้วยชั้นใหม่ ต้องค้นหา references ที่เสีย ต้องรัน syntax check, contract tests และ smoke test ของ route ที่เกี่ยวข้อง หากเป็น UI ต้องตรวจ desktop และ mobile breakpoint อย่างน้อย 320px, 390px, 430px, 768px และ viewport หลักของแอป

หากปุ่มมีการเขียนข้อมูลจริง ต้องตรวจว่ามี loading/disabled state, success feedback, error feedback และ reload/re-fetch state จาก source จริง ห้ามรายงานว่าฟีเจอร์ใช้งานได้เพียงเพราะปุ่มแสดงผลหรือ modal เปิดได้

## ข้อยกเว้น

การเพิ่ม compatibility shim ทำได้เฉพาะเมื่อมี legacy URL หรือ native bridge ที่ยังถูกใช้งานจริง และต้องมีวัน/เงื่อนไขนำออกที่ชัดเจน การใช้ third-party library ทำได้เมื่อไม่มี implementation เดิมที่รองรับและต้องไม่ซ้อนกับ dependency เดิมโดยไม่จำเป็น

## เกณฑ์การอนุมัติ

การเปลี่ยนแปลงถือว่าเสร็จเมื่อ source เดิมมี behavior ที่ต้องการ, ไม่มี implementation ซ้ำที่ยังถูกโหลด, ทุก references ถูกตรวจ, tests ผ่าน และ production route ที่เกี่ยวข้องเปิดได้จริง หากยังไม่ได้ตรวจ end-to-end ต้องระบุเป็นข้อจำกัดอย่างตรงไปตรงมา
