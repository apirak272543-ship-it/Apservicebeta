# ผลตรวจประสบการณ์มือถือ

หน้า Customer เวอร์ชันล่าสุดยังคงกำหนด viewport สำหรับมือถือ และเพิ่มกฎเฉพาะจอเล็กให้หน้าร้านแสดงรายการเมนูเป็นการ์ดแนวตั้งที่อ่านง่ายขึ้น รวมถึงทำให้ตะกร้าและปุ่มตำแหน่งใช้งานเต็มความกว้างตามลำดับความสำคัญของงาน

ทดสอบเปิดหน้าต่างเลือกพิกัดจากหน้าเว็บล่าสุดแล้ว พบว่า modal เปิดสำเร็จ มีพื้นที่แผนที่ `mapPickerCanvas` และปุ่ม **กลับสู่ตำแหน่งปัจจุบัน** แสดงพร้อมใช้งาน การทำงานจริงของ GPS ยังต้องอาศัยการอนุญาตสิทธิ์ตำแหน่งจากอุปกรณ์ของผู้ใช้
