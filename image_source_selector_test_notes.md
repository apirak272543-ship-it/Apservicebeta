# ผลตรวจปุ่มเลือกแหล่งรูปภาพ

ตรวจจากไฟล์ `ap-service-location-options.html` บนเบราว์เซอร์เมื่อ 14 สิงหาคม 2026 พบว่าในแท็บ **จัดการหน้าเว็บและสื่อ** ของ Admin ทุกช่องสื่อที่แสดงใน Content Studio มีปุ่มแยก 2 ปุ่มใต้ช่องรับภาพแล้ว ได้แก่ **เลือกจากคลังไฟล์** และ **ถ่ายรูปด้วยกล้อง** รวมถึงมีข้อความสถานะว่า `ยังไม่ได้เลือกภาพ` ก่อนเลือกไฟล์

การทดสอบนี้ยืนยันว่าตัวเลือกแบบใหม่ถูกสร้างใน DOM ของหน้า Customer/Admin ได้จริง ไม่ใช่เพียงข้อความคำแนะนำหรือ input ที่มี `capture` บังคับกล้อง

ได้เปิดไฟล์ Rider และ Store ในเบราว์เซอร์ด้วยเช่นกัน ทั้งสองคอนโซลโหลดหน้าเข้าสู่ระบบโดยไม่เกิดข้อผิดพลาด JavaScript จากตัวช่วยเลือกภาพเวอร์ชันใหม่ ส่วนช่องรูปภาพของแต่ละบทบาทจะถูกสร้างหลังผ่านการเข้าสู่ระบบ และได้รับสคริปต์มาตรฐานเดียวกันจาก `ImageSourceChoices` และ `StoreImageSourceChoices`
