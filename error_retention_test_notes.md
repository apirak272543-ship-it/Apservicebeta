# ผลตรวจระบบประหยัดพื้นที่

- หน้า Customer/Admin โหลดได้ตามปกติและมีฟังก์ชัน `runErrorRetentionCleanup` สำหรับ Master Admin
- เมื่อโหลดหน้าเว็บ ระบบไม่ส่งคำขอไปยัง `error-retention-cleanup` เอง จึงไม่เพิ่มภาระเครือข่ายขณะผู้ใช้ใช้งานปกติ
- งานล้างอัตโนมัติอยู่ที่ Supabase Cron วันละครั้ง โดยทดสอบ Edge Function สำเร็จและคืนผลว่าไม่มีภาพหรือ error log หมดอายุให้ลบในขณะทดสอบ
- ขอบเขตของงานล้างจำกัดเฉพาะ `error_reports` และ bucket `error-evidence`; ไม่มีคำสั่งอ่าน แก้ไข หรือลบข้อมูลลูกค้า ออร์เดอร์ การเงิน ร้านค้า หรือ Rider
